services:
  - type: web
    name: study-buddy
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Prompted for in the Render UI on first deploy — never stored in this repo.
      - key: AI_API_KEY
        sync: false
